/*finciones*/
/*fUNCtION NOMBREFUNCION
estructura: function nombrefuncion(){
   bloque de instruccion
}
*/
/*crear una funcion que indique si el numero ingresado por pantalla es par o impar, el programa se detiene cuando el usuario ingresa impar*/

function parimpar(){
    do{
        var numero = prompt(`ingresa un numero`)
        var estado = true
        if(numero == true){
            estado == false
        }
    }while(estado == true)
    
}

// esto es definir que va a hacer parimpar() para luego si usarla

/* ñas funciones son para crear tus propias funciones, lo que son pro ejerplo el prompt(), console.log(), estas son funciones que ya trae js por defecto y ya tienen definido que es lo que hacen pero podemos hacer nuestras propias funciones y especificarles que es lo que queremos que hagaan  para luego invocarlas */